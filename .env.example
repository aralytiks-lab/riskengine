# ── Database ──
DATABASE_URL=postgresql+asyncpg://riskengine:changeme@localhost:5433/lt_risk_engine

# ── Auth (Keycloak) ──
KEYCLOAK_URL=https://auth.leaseteq.ch/realms/leaseteq
KEYCLOAK_CLIENT_ID=risk-engine
KEYCLOAK_AUDIENCE=risk-engine-api

# ── Kafka ──
KAFKA_BOOTSTRAP=kafka:9092
KAFKA_TOPIC_RISK_EVENTS=risk.assessment.events

# ── Redis (rule caching) ──
REDIS_URL=redis://localhost:6380/0

# ── App ──
APP_ENV=development
LOG_LEVEL=INFO
SCORING_MODEL_VERSION=1.2
